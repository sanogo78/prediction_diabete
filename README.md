# Une plateforme de test de diabète avec la regression Logistic
# comment exécuté ce code ?
1. faire pip install -r requirements.txt dans votre terminal pour installer toutes les dépendence nécessaire.
   # démarrer le projet.
Faire : python manage.py runserver pour lancer le projet.
NB: mettre le DEBUG = True dans setting.
